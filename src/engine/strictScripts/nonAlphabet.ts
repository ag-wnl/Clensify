// Returns if the string contains suspicious usage of special characters or anything unecessary which might not be
// considered to potentially refer to something provocative

export const removeAnythingSuspicious = (inputString : string) : boolean => {
    
    

    return false;
}